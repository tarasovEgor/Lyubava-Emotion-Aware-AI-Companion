services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: ghcr.io/${IMAGE_OWNER:-local}/lyubava-emotion-aware-ai-companion:latest
    ports:
      - "8000:8000"
    environment:
      OPENROUTER_API_KEY: ${OPENROUTER_API_KEY:-}
